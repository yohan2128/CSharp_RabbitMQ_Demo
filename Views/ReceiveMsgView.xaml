<UserControl x:Class="CSharp_RabbitMQ_Demo.Views.ReceiveMsgView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="auto" Height="280"
             HorizontalAlignment="Left" VerticalAlignment="Top">
    <Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Received Messages" FontSize="16" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,12,0,0">
            <TextBlock Text="Host:" 
                       VerticalAlignment="Center" 
                       FontSize="14" 
                       Margin="5,0,12,0"/>
            <TextBox MinWidth="200"
                     Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     FontSize="14"
                     Margin="5,0,5,0"/>
            <TextBlock Text="Queue:" 
                       VerticalAlignment="Center" 
                       FontSize="14" 
                       Margin="5,0,12,0"/>
            <TextBox MinWidth="200"
                     Text="{Binding QueueName, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     FontSize="14"
                     Margin="5,0,5,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,12">
            <Button Content="Start service"
                    Command="{Binding StartServiceCommand}"
                    FontWeight="Bold"
                    FontSize="14"
                    Padding="12,6"
                    Margin="5,0,0,0"/>
            <Button Content="Stop service"
                    Command="{Binding StopServiceCommand}"
                    FontWeight="Bold"
                    FontSize="14"
                    Padding="12,6" Margin="12,0,0,0"/>
            <Button Content="Clear messages"
                    Command="{Binding RefreshCommand}"
                    FontWeight="Bold"
                    FontSize="14"
                    Padding="12,6" Margin="12,0,5,0"/>
        </StackPanel>

        <ListBox Grid.Row="3" ItemsSource="{Binding Messages}" Margin="0,4,0,0"
                 MinWidth="200" MaxWidth="360"
                 MinHeight="100" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <ListBox.Resources>
                <Style TargetType="ScrollBar">
                    <Setter Property="Width" Value="12"/>
                    <Setter Property="Background" Value="LightGray"/>
                    <Setter Property="Foreground" Value="Gray"/>
                </Style>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Padding="4"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
